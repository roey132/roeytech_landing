---
const {
  title = "תן לעסק לעבוד בשבילך",
  subtitle = `
בעזרת אוטומציות, אינטגרציות, בוטים ומערכות חכמות - העסק שלך ימשיך לפעול, גם כשאתה לא שם.
פחות עבודה - יותר תוצאות. 
  `,
  imageSrc = "/images/placeholder-image.png",
  buttonPrimary = { text: "בואו נתחיל יחד", href: "#contact" },
  tags = ["אוטומציות", "אינטגרציות", "בוטים", "עיבוד נתונים", "התאמה אישית"],
} = Astro.props;
---

<section class="purple-hero px-6 py-20">
  <div
    class="md:flex-col mx-auto flex max-w-7xl flex-col items-center justify-center gap-12"
  >
    <!-- text on the visual right -->
    <div class="flex text-right flex-col items-center justify-center">
      <h1
        class="underline-offset-10 underline decoration-10 decoration-violet-500 text-center text-5xl leading-snug font-bold text-[rgb(var(--foreground))] md:text-8xl"
      >
        {title}
      </h1>

      <p
        class="mt-4 max-w-2xl leading-snug text-center text-2xl text-[rgb(var(--primary))] "
      >
        {subtitle}
      </p>

      <!-- tags -->
      <div class="mt-6 flex flex-wrap justify-center gap-3">
        {
          tags.map((tag: string) => (
            <span class="purple-hue rounded-full border border-[rgb(var(--ring))] bg-[rgb(var(--background))] px-4 py-1 text-base text-[rgb(var(--foreground))]">
              {tag}
            </span>
          ))
        }
      </div>

      <!-- buttons -->
      <div class="mt-8 flex justify-center gap-4">
        <a
          href={buttonPrimary.href}
          class="inline-block rounded-lg bg-[rgb(var(--primary))] p-5px text-xl  px-15 py-3 font-medium text-[rgb(var(--primary-foreground))] transition hover:opacity-90"
        >
          {buttonPrimary.text}
        </a>
      </div>
    </div>
    <!-- image on the visual left (first in row) -->
    <div class="flex flex-1 justify-center">
      <div class="aspect-[1/1] w-full max-w-md">
        <!-- 1:1 square ratio -->
        <img
          src={imageSrc}
          alt="תמונה ראשית"
          class="h-full w-full rounded-2xl object-cover shadow-lg"
        />
      </div>
    </div>
  </div>
</section>
