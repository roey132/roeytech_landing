---
const {
  title = "קצת עליי",
  description = `
אני רועי, בונה מערכות ואוטומציות מותאמות אישית לעסקים קטנים ובינוניים.
אני אוהב לקחת תהליכים ידניים ומסורבלים ולהפוך אותם למערכת חכמה ואוטומטית.
עם ניסיון בשפות תכנות, אינטגרציות API, CRM מותאם, ודאשבורדים, אני מספק פתרונות
שמייעלים עבודה, חוסכים זמן, ומפחיתים טעויות.`,
  skills = ["אוטומציות", "אינטגרציות API", "CRM מותאם", "דאשבורדים", "בוטים"],
  cta = { text: "דברו איתי", href: "#contact" },
  imageSrc = "/images/placeholder-image.png",
} = Astro.props;
---

<section class="flex px-6 py-16">
  <div class="mx-auto flex max-w-5xl flex-col items-center gap-10 md:flex-row">
    <!-- Portrait -->
    <div class="flex-shrink-0">
      <div
        class="purple-hue aspect-[3/4] w-full max-w-[220px] overflow-hidden rounded-2xl shadow-lg"
      >
        <img
          src={imageSrc}
          alt="הפורטרט שלי"
          class="h-full w-full object-cover"
        />
      </div>
    </div>

    <!-- Text -->
    <div class="flex-1 text-right">
      <h2 class="mb-4 text-3xl font-bold text-[rgb(var(--foreground))]">
        {title}
      </h2>
      <p class="mb-6 text-lg leading-relaxed text-[rgb(var(--muted))]">
        {description}
      </p>

      <!-- Skills -->
      {
        skills.length > 0 && (
          <div class="justify-right mb-8 flex flex-wrap gap-3">
            {skills.map((skill: Text) => (
              <span class="purple-hue rounded-full border border-[rgb(var(--ring))] bg-[rgb(var(--background))] px-4 py-1 text-sm text-[rgb(var(--foreground))]">
                {skill}
              </span>
            ))}
          </div>
        )
      }

      <!-- Call to Action -->
      <a
        href={cta.href}
        class="align-center inline-block rounded-lg bg-[rgb(var(--primary))] px-6 py-3 font-medium text-[rgb(var(--primary-foreground))] transition hover:opacity-90"
      >
        {cta.text}
      </a>
    </div>
  </div>
</section>
