---
const {
  title = "למה לעבוד איתי",
  items = [
    {
      heading: "אישי, לא סוכנות",
      text: "תעבדו תמיד איתי. בלי העברות, בלי הפתעות."
    },
    {
      heading: "תוצאות לפני פיצ'רים",
      text: "מתמקדים בזמן שנחסך, פחות טעויות ודיווח ברור."
    },
    {
      heading: "תהליך פשוט",
      text: "שיחה קצרה - מיפוי - תכנון - עדכונים שבועיים - השקה ותמיכה."
    },
    {
      heading: "ליווי ארוך־טווח",
      text: "זמין לתקלות, התאמות ושיפורים אחרי ההשקה."
    },
  ],
} = Astro.props;
---

<section class="py-14 px-6">
  <div class="max-w-7xl mx-auto">
    <h2 class="text-center text-2xl md:text-3xl font-semibold text-[rgb(var(--foreground))] mb-6">
      {title}
    </h2>

    <div class="grid gap-5 sm:grid-cols-2 lg:grid-cols-4">
      {items.map(({heading, text }) => (
        <article
          class="purple-hue relative rounded-2xl border border-[rgb(var(--border))] bg-[rgb(var(--card))]/70 shadow-sm
                 px-6 py-6 text-right overflow-hidden group">
          
          <h3 class="relative z-10 mt-1 text-xl md:text-xl font-bold text-[rgb(var(--foreground))]">
            {heading}
          </h3>
          <p class="relative z-10 mt-1 text-md text-[rgb(var(--muted))]">
            {text}
          </p>
        </article>
      ))}
    </div>
  </div>
</section>
